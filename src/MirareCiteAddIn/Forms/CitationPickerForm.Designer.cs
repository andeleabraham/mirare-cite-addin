// ============================================================================
//  CitationPickerForm.Designer.cs  —  empty stub.  The real form layout is
//  hand-coded inside InitializeComponent() in CitationPickerForm.cs; this
//  stub only exists so the .csproj's <DependentUpon> resolves and the
//  VS designer doesn't choke.
// ============================================================================

namespace MirareCiteAddIn.Forms
{
    partial class CitationPickerForm
    {
        /// <summary>Required designer variable.</summary>
        private System.ComponentModel.IContainer components = null;

        protected override void Dispose(bool disposing)
        {
            if (disposing && components != null)
                components.Dispose();
            base.Dispose(disposing);
        }
    }
}
