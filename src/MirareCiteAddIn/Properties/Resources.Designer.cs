//------------------------------------------------------------------------------
// Properties\Resources.Designer.cs
//   Auto-generated stub — only here so the .csproj's <Compile Include=...>
//   resolves.  We do not actually use Resources.resx for the ribbon XML
//   (we load it from an embedded manifest stream — see Connect.cs).
//------------------------------------------------------------------------------

namespace MirareCiteAddIn.Properties {
    [System.Runtime.InteropServices.ComVisible(false)]
    internal class Resources {
        private static System.Resources.ResourceManager _rm;
        internal static System.Resources.ResourceManager ResourceManager {
            get {
                if (_rm == null) {
                    _rm = new System.Resources.ResourceManager(
                        "MirareCiteAddIn.Properties.Resources",
                        typeof(Resources).Assembly);
                }
                return _rm;
            }
        }
    }
}
